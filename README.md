# DarkBTC
A ZK-powered dark pool for Bitcoin that combines a private swap AMM, a sealed-bid auction mechanism, and a hidden orderbook — three features that share one underlying architecture: you commit to a trade intent with a ZK proof, execute without leaking counterparty, amount, or price on-chain.
